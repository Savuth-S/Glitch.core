[project]
requires-python = ">=3.14"
dependencies = ["pillow"]

name = "glitch-core"
version = "0.0.2"

license = "AGPL-3.0-or-later"
license-files = ["LICENSE"]

    [project.scripts]
    glitch-core = "glitch-core.__main__:main"